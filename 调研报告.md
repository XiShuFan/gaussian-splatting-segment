# CUDA

nvcc

nvidia-smi



# gaussian-splatting

高斯训练

```cmd
python train.py -s /xxx/undistorted --model_path /xxx/undistorted/3dgs
```

高斯测试，注意需要保持图片原始分辨率

```cmd
python render.py -s /xxx/undistorted -model_path /xxx/undistorted/3dgs -r 1
```





# SAM

可以在同一个虚拟环境，支持Python3.7

[facebookresearch/segment-anything: The repository provides code for running inference with the SegmentAnything Model (SAM), links for downloading the trained model checkpoints, and example notebooks that show how to use the model.](https://github.com/facebookresearch/segment-anything)

```cmd
git clone https://github.com/facebookresearch/segment-anything.git

pip install -e .

python scripts/amg.py \
       --checkpoint /checkpoints/sam_vit_h_4b8939.pth \
       --model-type vit_h \
       --input /xxx/scene/undistorted/images \
       --output /xxx/undistorted/images_seg
```

- **`default` or `vit_h`: [ViT-H SAM model.](https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth)**
- `vit_l`: [ViT-L SAM model.](https://dl.fbaipublicfiles.com/segment_anything/sam_vit_l_0b3195.pth)
- `vit_b`: [ViT-B SAM model.](https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth)





# U2Net

SOD前景分割：U^2-Net: Going Deeper with Nested U-Structure for Salient Object Detection.

[xuebinqin/U-2-Net: The code for our newly accepted paper in Pattern Recognition 2020: "U^2-Net: Going Deeper with Nested U-Structure for Salient Object Detection."](https://github.com/xuebinqin/U-2-Net)

```cmd
git clone https://github.com/NathanUA/U-2-Net.git

// 下载预训练模型
// 把需要分割前景的图片放到 test_data 文件夹

python u2net_test.py
```

Download the pre-trained model u2net.pth (176.3 MB) from [**GoogleDrive**](https://drive.google.com/file/d/1ao1ovG1Qtx4b7EoskHXmi2E9rp5CHLcZ/view?usp=sharing) or [**Baidu Pan 提取码: pf9k**](https://pan.baidu.com/s/1WjwyEwDiaUjBbx_QxcXBwQ) or u2netp.pth (4.7 MB) from [**GoogleDrive**](https://drive.google.com/file/d/1rbSTGKAE-MTxBYHd-51l2hMOQPT_7EPy/view?usp=sharing) or [**Baidu Pan 提取码: 8xsi**](https://pan.baidu.com/s/10tW12OlecRpE696z8FxdNQ) and put it into the dirctory './saved_models/u2net/' and './saved_models/u2netp/'





# DIS（前景分割）

U2Net后续工作，效果更好。

[xuebinqin/DIS: This is the repo for our new project Highly Accurate Dichotomous Image Segmentation](https://github.com/xuebinqin/DIS?tab=readme-ov-file)

** (2022-Aug.-17)** The optimized model for general use of our IS-Net is now released: `isnet-general-use.pth` (for general use, this is NOT DIS V2.0.) from [(Google Drive)](https://drive.google.com/file/d/1XHIzgTzY5BQHw140EDIgwIb53K659ENH/view?usp=sharing) or [(Baidu Pan 提取码:6jh2)](https://pan.baidu.com/s/111MqmwnUc8Z4Wsq2Pc4bhQ?pwd=6jh2), please feel free to try it with the newly created simple `inference.py` code on your own datasets.

```cmd
git clone https://github.com/xuebinqin/DIS.git

// 下载模型权重，放到文件夹 saved_models/IS-Net

python inference.py
```



# BiRefNet

[ZhengPeng7/BiRefNet: [CAAI AIR'24\] Bilateral Reference for High-Resolution Dichotomous Image Segmentation](https://github.com/ZhengPeng7/BiRefNet)



# colmap

COLMAP是一个通用的运动结构（SfM）和多视图立体（MVS）处理工具，具有图形和命令行界面。它为有序和无序图像集合的重建提供了广泛的功能。

## 安装教程

教程：[Installation — COLMAP 3.13.0.dev0 | a5332f46 (2025-07-05) documentation](https://colmap.github.io/install.html)

### 步骤1：升级cmake到最新版本

```cmd
# 卸载旧版本
sudo apt-get remove --purge cmake
sudo apt update
sudo apt install -y software-properties-common lsb-release
sudo apt-add-repository 'deb https://apt.kitware.com/ubuntu/ focal main'
wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc | sudo apt-key add -
sudo apt update
sudo apt install -y cmake
```



### 步骤2：安装依赖包

```cmd
sudo apt-get install \
    ninja-build \
    build-essential \
    libboost-program-options-dev \
    libboost-graph-dev \
    libboost-system-dev \
    libeigen3-dev \
    libfreeimage-dev \
    libmetis-dev \
    libgoogle-glog-dev \
    libgtest-dev \
    libgmock-dev \
    libsqlite3-dev \
    libglew-dev \
    qtbase5-dev \
    libqt5opengl5-dev \
    libcgal-dev \
    libcurl4-openssl-dev \
    libmkl-full-dev
```



### 步骤3：手动安装glog、ceres

```cmd
sudo apt-get remove --purge libgoogle-glog-dev
git clone https://github.com/google/glog.git
cd glog
git checkout v0.7.0
mkdir build && cd build
cmake ..
make -j$(nproc)
sudo make install
```

```cmd
sudo apt-get remove --purge libceres-dev
git clone https://github.com/ceres-solver/ceres-solver.git
cd ceres-solver
git checkout 2.2.0
mkdir build && cd build
cmake ..
make -j$(nproc)
sudo make install
```



### 步骤4：更新索引

```cmd
sudo updatedb
```





### 步骤5：配置并编译colmap

首先要自行安装需要的cuda版本

```cmd
git clone https://github.com/colmap/colmap.git
cd colmap
mkdir build
cd build
cmake .. -GNinja -DBLA_VENDOR=Intel10_64lp -DCMAKE_CUDA_ARCHITECTURES=native -DCMAKE_CXX_FLAGS="-Wno-error"
ninja -v
sudo ninja install
```



### 步骤6：运行colmap

```cmd
colmap -h
colmap gui
```



## 使用教程

传统上，基于图像的3D重建首先使用Structure-from-Motion恢复场景的稀疏表示和输入图像的相机姿势。然后，这个输出作为多视图立体（MVS）的输入，以恢复场景的密集表示。

### 步骤1：稀疏/密集重建

可用参数

```cmd
$ colmap automatic_reconstructor -h
  -h [ --help ] 
  --default_random_seed arg (=0)
  --log_to_stderr arg (=1)
  --log_level arg (=0)
  --project_path arg
  --workspace_path arg
  --image_path arg
  --image_list_path arg
  --mask_path arg
  --vocab_tree_path arg 
  --data_type arg (=individual)         {individual, video, internet}
  --quality arg (=high)                 {low, medium, high, extreme}
  --camera_model arg (=SIMPLE_RADIAL)
  --single_camera arg (=0)
  --single_camera_per_folder arg (=0)
  --camera_params arg
  --extraction arg (=1)
  --matching arg (=1)
  --sparse arg (=1)
  --dense arg (=1)
  --mesher arg (=poisson)               {poisson, delaunay}
  --num_threads arg (=-1)
  --random_seed arg (=-1)
  --use_gpu arg (=1)
  --gpu_index arg (=-1)

```



```cmd
# The project folder must contain a folder "images" with all the images.
$ DATASET_PATH=/path/to/project

$ colmap automatic_reconstructor \
    --workspace_path $DATASET_PATH \
    --image_path $DATASET_PATH/images
    --dense 0
```

### 步骤2：

Gaussian Splatting 的官方实现只支持 **无畸变的相机内参模型**，所以必须先进行 **undistortion（去畸变）**。

```cmd
colmap image_undistorter \
    --image_path path/to/images \
    --input_path path/to/sparse/0 \
    --output_path path/to/undistorted \
    --output_type COLMAP

```



## 强制所有图片合并

非常好的问题，这其实是 **COLMAP 自动重建流程中最常见的“坑”之一**。你看到的 `sparse/0`, `sparse/1`, `sparse/2` 等文件夹确实表示 **多个独立的稀疏重建模型（reconstruction models）**。COLMAP 在自动流程（`automatic_reconstructor`）中，会 **自动聚类相机位姿与匹配关系**，为每个能成功配准的独立图像集生成一个模型。

------

## 🔍 产生多个 sparse 文件夹的原因

即使你确认是同一个场景，COLMAP 仍可能会分出多个模型。常见原因包括：

1. **图像之间缺乏足够的重叠**（无法三角化到共同的结构）
2. **光照变化大** 或 **模糊/反光表面**
3. **EXIF 相机参数差异** 导致无法估计共同内参
4. **特征匹配阈值过严**
5. **部分图像完全未匹配上任何其他图像**

------

## ✅ 目标：强制 COLMAP 尝试“处理所有图片”（只生成一个模型）

你可以通过改用更底层的命令替代 `automatic_reconstructor`，从而对匹配与重建的参数做控制。以下是推荐做法：

------

### **方案 A：修改 matching 阶段，让匹配更“宽松”**

先清理旧结果：

```bash
rm -rf $DATASET_PATH/sparse
rm -rf $DATASET_PATH/database.db
```

然后手动执行：

```bash
colmap feature_extractor \
    --database_path $DATASET_PATH/database.db \
    --image_path $DATASET_PATH/images

colmap exhaustive_matcher \
    --database_path $DATASET_PATH/database.db
```

> 🔸 这一步用的是 *exhaustive matcher*（穷举匹配），即让每张图像和每张图像都尝试匹配。
> 对小数据集（<500 张）非常适合。

------

然后再运行：

```bash
mkdir $DATASET_PATH/sparse

colmap mapper \
    --database_path $DATASET_PATH/database.db \
    --image_path $DATASET_PATH/images \
    --output_path $DATASET_PATH/sparse \
    --Mapper.ba_global_max_num_iterations 50 \
    --Mapper.tri_merge_max_reproj_error 8.0 \
    --Mapper.min_num_matches 15
```

> 你可以调整：
>
> - `--Mapper.min_num_matches`（默认15）→ 设置得更低（比如 10）能强制更多图片加入
> - `--Mapper.init_min_num_inliers`（默认100）→ 也可以降低，比如 50

------

### **方案 B：合并多个模型**

如果 COLMAP 仍输出了多个 `sparse/*` 文件夹，你可以尝试合并它们：

```bash
colmap model_merger \
    --input_path1 $DATASET_PATH/sparse/0 \
    --input_path2 $DATASET_PATH/sparse/1 \
    --output_path $DATASET_PATH/sparse/merged \
    --merge_type triangulation
```

> 不过要注意，这种合并通常要求两个模型至少有少量重叠视角，否则无法对齐。

------

### **方案 C：使用 sequential matcher（如果是视频帧或顺序拍摄）**

如果你的图片来自 **视频帧序列** 或 **沿路径拍摄**，可以用：

```bash
colmap sequential_matcher \
    --database_path $DATASET_PATH/database.db \
    --image_path $DATASET_PATH/images \
    --SiftMatching.guided_matching 1
```

这样只匹配相邻帧，鲁棒性更高，也能避免被拆成多个子模型。

------

## ⚙️ 总结推荐命令序列（更“强制”地处理所有图像）

```bash
# 1. 特征提取
colmap feature_extractor \
    --database_path $DATASET_PATH/database.db \
    --image_path $DATASET_PATH/images

# 2. 尽量宽松地匹配（根据场景选一种）
colmap exhaustive_matcher --database_path $DATASET_PATH/database.db
# 或者：
# colmap sequential_matcher --database_path $DATASET_PATH/database.db --image_path $DATASET_PATH/images

# 3. 稀疏重建
mkdir -p $DATASET_PATH/sparse
colmap mapper \
    --database_path $DATASET_PATH/database.db \
    --image_path $DATASET_PATH/images \
    --output_path $DATASET_PATH/sparse \
    --Mapper.ba_global_max_num_iterations 50 \
    --Mapper.min_num_matches 10 \
    --Mapper.init_min_num_inliers 50 \
    --Mapper.abs_pose_min_num_inliers 10
```

这样能最大化地让 COLMAP 尝试把所有图片拼进同一个模型。

------

是否方便告诉我：

- 你的数据集大约有多少张图片？
- 图片是视频帧（连续拍摄）还是手持随意拍摄的？

我可以根据这些进一步帮你设置更合适的 matcher 类型与参数组合（比如 hybrid_matcher / vocab_tree_matcher）。





# 打包docker镜像





# 执行步骤

## 步骤1：视频抽帧

10线程，抽取100张帧图片，耗时0.5分钟

```cmd
python extract_frame_from_video.py /media/why/新加卷/xsf/商品3DGS/scene/pooh_bear.mp4 --output /media/why/新加卷/xsf/商品3DGS/scene/images
```



## 步骤2：稀疏点云匹配

高质量重建，耗时1.5分钟

```cmd
colmap automatic_reconstructor \
--workspace_path /media/why/新加卷/xsf/商品3DGS/scene \
--image_path /media/why/新加卷/xsf/商品3DGS/scene/images \
--dense 0 \
--quality extreme
```

测试

```cmd
colmap automatic_reconstructor \
--workspace_path /media/why/新加卷/xsf/商品3DGS/shirt \
--image_path /media/why/新加卷/xsf/商品3DGS/shirt/images
```



## 步骤3：去除畸变

耗时10秒

```cmd
colmap image_undistorter \
--image_path /media/why/新加卷/xsf/商品3DGS/scene/images \
--input_path /media/why/新加卷/xsf/商品3DGS/scene/sparse/0 \
--output_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted \
--output_type COLMAP
```

测试

```cmd
colmap image_undistorter \
--image_path /media/why/新加卷/xsf/商品3DGS/shirt/images \
--input_path /media/why/新加卷/xsf/商品3DGS/shirt/sparse/0 \
--output_path /media/why/新加卷/xsf/商品3DGS/shirt/undistorted \
--output_type COLMAP
```





## 步骤4：构造sparse/0文件夹

0耗时

```cmd
mkdir -p /media/why/新加卷/xsf/商品3DGS/scene/undistorted/sparse/0
mv /media/why/新加卷/xsf/商品3DGS/scene/undistorted/sparse/!(0) /media/why/新加卷/xsf/商品3DGS/scene/undistorted/sparse/0/
```

测试

```cmd
mkdir -p /media/why/新加卷/xsf/商品3DGS/shirt/undistorted/sparse/0
mv /media/why/新加卷/xsf/商品3DGS/shirt/undistorted/sparse/!(0) /media/why/新加卷/xsf/商品3DGS/shirt/undistorted/sparse/0/
```



## 前景分割

```cmd
python infer_script.py \
--src_dir '/media/why/新加卷/xsf/商品3DGS/shirt/undistorted/images' \
--dst_dir '/media/why/新加卷/xsf/商品3DGS/shirt/undistorted/masks'
```



## 深度估计

```cmd
python prepare_colmap_depth.py \
--depth_maps_dir "/media/why/新加卷/xsf/商品3DGS/shirt/dense/0/stereo/depth_maps" \
--images_dir "/media/why/新加卷/xsf/商品3DGS/shirt/dense/0/images" \
--output_dir "/media/why/新加卷/xsf/商品3DGS/shirt/undistorted/depths" \
--json_dir "/media/why/新加卷/xsf/商品3DGS/shirt/undistorted/sparse/0/"
```





## 步骤5：高斯训练

使用图像原始分辨率训练，7k步。7分钟

这一步骤很慢，探索一下多卡训练？

```cmd
python train.py \
--source_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted \
--model_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted/3dgs \
--images images \
--masks masks \
--depths depths \
--resolution 1 \
--iterations 7000 \
--sh_degree 0 \
--densification_interval 80 \
--opacity_reset_interval 2000
```



测试指令

```cmd
python train.py \
--source_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted \
--model_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted/test \
--images images \
--masks masks \
--depths depths \
--resolution 2 \
--iterations 7000 \
--sh_degree 0 \
--densification_interval 50 \
--opacity_reset_interval 1000
```



高斯训练时加一些正则化损失，防止过拟合

| 目标   | 正则化思路                          | 效果               |
| ------ | ----------------------------------- | ------------------ |
| 颜色   | 约束到中性范围或平滑邻域            | 避免颜色发散       |
| 尺度   | 限制范围或鼓励均匀                  | 防止高斯膨胀或塌陷 |
| 透明度 | 可加 `opacity * (1 - opacity)` 惩罚 | 防止透明度饱和     |
| 位置   | 可加邻域平滑                        | 防止高斯漂移不稳定 |

加上深度损失。





## 步骤6：分割前景

0.5分钟，可以同步进行

```cmd
python u2net_test.py
```



## 步骤7：光栅化像素与高斯的对应关系

5分钟

```cmd
python render.py -s /media/why/新加卷/xsf/商品3DGS/scene/undistorted --model_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted/3dgs -r 1
```

测试脚本

```cmd
python render.py -s /media/why/新加卷/xsf/商品3DGS/scene/undistorted --model_path /media/why/新加卷/xsf/商品3DGS/scene/undistorted/test -r 1
```





## 步骤：分割视频帧

```cmd
python scripts/amg.py \
       --checkpoint checkpoints/sam_vit_h_4b8939.pth \
       --model-type vit_h \
       --input /media/why/新加卷/xsf/商品3DGS/scene/undistorted/images \
       --output /media/why/新加卷/xsf/商品3DGS/scene/undistorted/images_seg
```







# colmap、nerf、3dgs的区别

问得非常好！这是一个核心问题，触及了现代3D计算机视觉的发展脉络。

简单直接的答案是：**COLMAP（传统方法）和NeRF、Gaussian Splatting（神经渲染方法）解决的是不同层次的问题。后者存在的意义在于，它们能实现COLMAP难以达到的**“渲染质量”**和**“灵活性”**，开辟了全新的应用场景。

让我们来详细拆解一下：

### 1. COLMAP：卓越的几何重建专家

COLMAP代表了传统多视图立体视觉的巅峰。它的工作流程非常清晰：

*   **输入**：一系列图片。
*   **输出**：稀疏点云（相机位置+特征点）、稠密点云/网格（3D几何）。
*   **核心任务**：**精确地恢复三维几何和相机姿态**。

**COLMAP的局限性：**

1.  **离散表示**：它的输出是点云或网格。点云本身不包含表面信息，而网格化是一个容易出错的步骤，尤其是在细节复杂或遮挡严重的区域。
2.  **视图合成质量差**：如果你用COLMAP重建的网格，贴上纹理，然后从一个*新的*、训练集中没有的视角去渲染，效果通常会比较差。你会看到纹理模糊、接缝、扭曲，因为它本质上是在“粘贴”现有图片，无法理解光线和材质的连续性。
3.  **对复杂外观建模能力弱**：COLMAP主要依赖光度一致性假设。对于反光、透明、无纹理、非朗伯体表面，它的重建会失败或产生大量噪声。它无法分离“几何”和“外观”（如光照、材质）。
4.  **存储与渲染效率**：高质量的密集点云或网格文件体积巨大，实时渲染需要复杂的图形学优化。

**一句话总结COLMAP：它给出了一个**“硬”的3D模型**，但这个模型的**视觉保真度是有限的**。

---

### 2. NeRF：场景的“隐式记忆”与超逼真合成

NeRF带来了一场范式革命。它不直接输出点云或网格，而是训练一个巨大的神经网络。

*   **输入**：图片 + 对应的相机参数（**通常可以由COLMAP先计算出来！**）。
*   **输出**：一个神经网络，这个网络学会了整个场景的“5D函数”——对于空间中的任意一个点 `(x, y, z)` 和观察方向 `(θ, φ)`，它能预测该点的颜色 `(r, g, b)` 和密度 `σ`。
*   **核心任务**：**学习一个连续的体积场景表示，以实现任意视角的照片级真实感渲染**。

**NeRF存在的核心意义：**

1.  **无与伦比的视图合成质量**：这是NeRF的杀手锏。从任何新视角渲染出的图片都极其逼真、连续、无缝，具有正确的视差和细节。这是COLMAP完全无法做到的。
2.  **连续表示**：它表示的是一个连续的3D空间，而不是离散的点或三角形。因此，它可以表示极其复杂的几何和外观（如头发、烟雾），并且渲染分辨率在理论上是无限的。
3.  **处理复杂外观**：通过将观察方向作为输入，神经网络可以学会视角相关的效果，比如镜面高光、半透明，从而能很好地重建反光表面。
4.  **隐式正则化**：神经网络本身作为一种强大的先验，能够“脑补”出被遮挡或噪声区域的合理细节，产生非常平滑和完整的结果。

**NeRF的缺点**：训练和渲染速度极慢（尽管后续研究已大幅改进）。

---

### 3. 3D Gaussian Splatting：两全其美的“游戏规则改变者”

Gaussian Splatting可以看作是NeRF思想和传统计算机图形学 rasterization（光栅化）的精妙结合。它取得了质量和速度的绝佳平衡。

*   **输入**：图片 + 相机参数（**同样，通常先由COLMAP提供**）。
*   **输出**：一堆可学习的、带有颜色和透明度的3D高斯椭球体。
*   **核心任务**：**用一种显式的、可微分的数据结构来表示场景，以实现实时、照片级真实感的渲染。**

**Gaussian Splatting存在的核心意义：**

1.  **实时渲染 + 顶级质量**：这是它最震撼的地方。它能在高分辨率下达到每秒数百帧的渲染速度，同时视觉质量媲美甚至超过NeRF。这对于VR/AR、游戏等实时应用是革命性的。
2.  **显式表示**：与NeRF的“黑箱”神经网络不同，它的表示是显式的（一堆高斯球），非常直观，易于编辑和压缩。
3.  **训练速度快**：相比早期的NeRF，它的训练速度极快（几分钟到几十分钟）。
4.  **细节丰富**：它能捕捉到非常精细的细节，表现力极强。

**它的缺点**：对非常无纹理的区域重建可能不够稳健，并且存储需求相对较大（但可通过压缩技术解决）。

---

### 总结与类比

为了让你更好地理解，我们可以做一个类比：

*   **COLMAP** 就像一个**出色的建筑测量师**。他可以用激光扫描仪精确地测量出房子的长、宽、高、结构，给你一套非常准确的**建筑蓝图（点云/网格）**。但根据这套蓝图，你无法感受到房子在午后阳光下的温暖氛围。

*   **NeRF** 就像一个**拥有绝对记忆的天才画家**。他坐在房子里看一整天，然后回到画室。你告诉他从任何一个窗户看出去，他都能给你画出一幅**照片般逼真的油画**，连光影、反射都分毫不差。但他作画（渲染）很慢。

*   **3D Gaussian Splatting** 则像一个**掌握了最新CG技术的特效团队**。他们先用测量师的数据搭好基础，然后用无数个微小的、带颜色的粒子（高斯球）来填充这个空间。最终，他们可以在**电脑上实时地、以假乱真地渲染**出这个房子的任何视角，让你仿佛身临其境。

### 协同而非取代：一个典型的现代工作流

实际上，在现代3D重建中，这些技术是**协同工作**的，形成了一个强大的流水线：

1.  **COLMAP（几何基础）**：依然是不可或缺的第一步。它负责从原始图片中**可靠且高效地计算出精确的相机姿态**。这是NeRF和Gaussian Splatting能够成功训练的**基石**。没有准确的相机参数，它们的效果会大打折扣。
2.  **NeRF / Gaussian Splatting（高质量渲染）**：利用COLMAP提供的相机参数，学习场景的外观和精细几何，生成一个可以用于高质量自由视角渲染的模型。

**结论：**

NeRF和Gaussian Splatting存在的意义，**不是取代COLMAP的几何重建能力，而是超越它**。它们的目标是生成一个不仅仅是几何正确，更是**视觉上完美**的数字化身。它们将3D重建的重点从“获取一个可用的3D模型”提升到了“创造一种令人信服的、沉浸式的视觉体验”，这为数字孪生、虚拟现实、影视特效和文化遗产数字化等领域带来了前所未有的可能性。

